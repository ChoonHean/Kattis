#include<bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    if (n > 1)cout << "blandad best";
    else {
        string s;
        cin >> s;
        cout << s;
    }
    return 0;
}